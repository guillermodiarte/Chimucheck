version: '3'

services:
  chimucheck-web:
    container_name: chimucheck-web
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/prod.db
    volumes:
      - ./public/uploads:/app/public/uploads
      - ./data:/app/data
